function show_hide_button(dev_id) {
  if (document.getElementById('dev_id').style.display == "block")
       {
        document.getElementById('dev_id').style.display = "none";       
      }
  else
  {
     document.getElementById('dev_id').style.display = "block";   
  }
}